#include <stdio.h>
#include <conio.h>

int main(void) {

     int a, b, c;
    double r,p;
    scanf("%d", &a);
    c = a%2;
    if (/* condition */)
    {
        /* code */
    }
    
    ch = a/2;
   if  
    printf("%.2lf", rad);
    return 0;
}
  
